# 隐私与权限

DeskKit 没有内置遥测、广告或作者运营的数据收集服务。它处理的数据取决于你启用的组件。

- **系统组件**读取网络字节计数、CPU、内存和磁盘容量，不读取网络报文或磁盘文件内容。
- **Codex 组件**通过本机 Codex 客户端查询额度。客户端处理自己的登录与网络请求；DeskKit 不打包账号凭据。不要把自己的 Codex 目录放入组件包。
- **HTTP 组件**访问配置的地址，对方会看到正常网络请求信息（例如 IP 和 User-Agent）。DeskKit 的 HTTP 会话不持久保存 Cookie 或响应缓存。
- **command 组件**能够以当前用户权限运行配置中的程序。这不是安全沙盒。只启用你信任并检查过的命令；命令可能读取文件、联网或调用其他程序。子进程只继承必要的路径与语言环境，不自动继承环境变量中的令牌和密码。
- 布局 JavaScript 在独立进程中执行，具有超时限制，没有提供文件、网络或命令桥接接口。这是稳定性边界，不是对恶意代码的安全保证。

## 本地保存

组件脚本和配置保存在 `~/Library/Application Support/DeskKit/Plugins/`。设置保存在本机偏好设置中。桌面组件需要的目录与展示快照保存在本机构建对应的 App Group 容器中；快照可能包含你选择展示的私人内容，没有额外加密。

诊断文件只保留时间和数量统计，不写入本机目录、组件名称/ID、硬件读数或原始错误。供桌面组件使用的缓存不保存原始错误文本；详细错误可能临时显示在应用中。系统日志中的错误详情标记为私密。

这些文件不会由 DeskKit 自动上传。反馈问题前请检查截图、日志、配置中的路径、地址和私人内容。删除组件会移除其展示快照并把组件包移入废纸篓；其他备份或系统保存的数据需要自行管理。

## 公开包的边界

仓库和样例不包含作者的登录令牌、运行缓存、私人目录或本地签名配置。Andreas 是公开署名。通过 Apple 证书签名的安装包仍可被检查到证书身份及 Team ID，它们不等同于私钥。

三个样例默认停用，仅作参考。数据源地址、Codex 安装位置与网卡选择都可能需要下载者自行调整。请勿把密钥写入准备分享的 URL、命令参数或组件配置。
